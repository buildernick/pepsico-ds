import { FC } from 'react';
import { IButtonGroupProps } from './types';

/**
 * Used to group Buttons together without worrying about layout
 */
export declare const ButtonGroup: FC<IButtonGroupProps>;
export default ButtonGroup;
