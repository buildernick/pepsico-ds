import { DashboardCardProps } from './types';

declare const DashboardCard: ({ isDark: darkOverride, pinned, centralIcon, title, subTitle, ctaPrimary, ctaSecondary, link1, link2, link3, style, className, }: DashboardCardProps) => import("react/jsx-runtime").JSX.Element;
export default DashboardCard;
