/**
 * Assign this value to a story "parameters" prop to enable easy dark theme integration;
 * when used, story does NOT need to set "isDark: true" in "args" prop
 */
export declare const DARK_THEME_PARAMETERS: {
    backgrounds: {
        default: string;
    };
};
